package com.tersesystems.logback.proxy;

import org.slf4j.Marker;

import java.util.function.*;
import java.util.Optional;

public interface ConditionalLogger @ob
    @for(level <- Seq(
      Level("trace", "Trace"),
      Level("debug", "Debug"),
      Level("info", "Info"),
      Level("warn", "Warn"),
      Level("error", "Error"),
    )) {
    void if@{level.capitalizedName}(Supplier<Boolean> condition, Consumer<LoggerStatement> lc);
    void if@{level.capitalizedName}(Marker marker, Supplier<Boolean> condition, Consumer<LoggerStatement> lc);
    Optional<LoggerStatement> if@{level.capitalizedName}(Supplier<Boolean> condition);
    Optional<LoggerStatement> if@{level.capitalizedName}(Marker marker, Supplier<Boolean> condition);
    }
@cb