package com.tersesystems.logback.proxy;

import org.slf4j.Logger;
import org.slf4j.Marker;
import org.slf4j.event.Level;
import java.util.function.Consumer;
import java.util.function.Supplier;
import java.util.function.Predicate;
import java.util.Optional;

@* "sbt generateSources" autogenerates in target/sources/txt.Results *@
public interface LazyLogger @ob

    @for(level <- Seq(
      Level("trace", "Trace"),
      Level("debug", "Debug"),
      Level("info", "Info"),
      Level("warn", "Warn"),
      Level("error", "Error"),
    )) {

    void @{level.name}(Consumer<LoggerStatement> lc);
    Optional<LoggerStatement> @{level.name}();

    void @{level.name}(Marker marker, Consumer<LoggerStatement> lc);
    Optional<LoggerStatement> @{level.name}(Marker marker);
    }
@cb