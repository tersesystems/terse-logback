include "logback-reference"

log.dir = "target/log"

local {
  logback.environment=development

  censor {
    text.enabled = false
    json.enabled = false
  }

  textfile {
    append = false
    immediateFlush = true
  }

  jsonfile {
    append = false
    immediateFlush = true
  }
}
