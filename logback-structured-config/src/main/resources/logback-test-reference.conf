include "logback-reference"

log.dir = "target/log"

local {
  censor {
    text.enabled = false
    json.enabled = false
  }

  console {
    enabled = false
  }

  jsonfile {
    enabled = false
  }

  textfile {
    enabled = false
  }
}
